$kind: http-request
method: POST
url: '{{baseUrl}}/users'
order: 2000
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "name": "string",
      "email": "user@example.com",
      "role": "admin",
      "status": "active"
    }
